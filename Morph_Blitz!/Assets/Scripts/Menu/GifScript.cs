using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class GifScript : MonoBehaviour 
{

	public RawImage[] frames;

	private float FPS = 2;
	private int index;

	// Use this for initialization
	void Start () 
	{
		frames = gameObject.GetComponent<Texture> ();

	}
	
	// Update is called once per frame
	void Update () 
	{
		index = (int)(Time.deltaTime * FPS) % frames.Length;
		gameObject.GetComponent<Material> ().mainTexture = frames[index];
	}
}*/
